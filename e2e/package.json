{
  "name": "ssh-browser-e2e",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Opens the same bytes through the daemon and over file://, in a real browser.",
  "scripts": {
    "browser": "playwright install --with-deps chromium",
    "e2e": "node run.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
